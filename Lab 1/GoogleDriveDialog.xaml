<Window x:Class="Lab_1.GoogleDriveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Lab_1"
        Title="Google Drive - Електронні таблиці" Height="500" Width="700"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <local:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Статус автентифікації -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" x:Name="StatusTextBlock" 
                           Text="Не підключено до Google Drive" 
                           VerticalAlignment="Center" FontWeight="Bold"/>

                <Button Grid.Column="1" x:Name="AuthButton" 
                        Content="Підключитися" Width="120" 
                        Click="AuthButton_Click"/>
            </Grid>
        </Border>

        <!-- Панель інструментів -->
        <ToolBar Grid.Row="1" Margin="0,0,0,10">
            <Button x:Name="RefreshButton" Content="🔄 Оновити" 
                    Click="RefreshButton_Click" IsEnabled="False"/>
            <Separator/>
            <Button x:Name="UploadButton" Content="⬆ Завантажити" 
                    Click="UploadButton_Click" IsEnabled="False"/>
            <Button x:Name="DownloadButton" Content="⬇ Відкрити" 
                    Click="DownloadButton_Click" IsEnabled="False"/>
            <Separator/>
            <Button x:Name="DeleteButton" Content="🗑 Видалити" 
                    Click="DeleteButton_Click" IsEnabled="False"/>
        </ToolBar>

        <!-- Список файлів -->
        <Grid Grid.Row="2" Margin="10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Файли на Google Drive:" 
                       FontWeight="Bold" Margin="0,0,0,5"/>

            <DataGrid Grid.Row="1" x:Name="FilesDataGrid" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionChanged="FilesDataGrid_SelectionChanged"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#F9F9F9">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Назва" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Дата зміни" Binding="{Binding DisplayModified}" Width="150"/>
                    <DataGridTextColumn Header="Розмір" Binding="{Binding DisplaySize}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Повідомлення про порожній список -->
            <Border Grid.Row="1" Background="White" 
                    Visibility="{Binding ElementName=FilesDataGrid, Path=Items.Count, Converter={StaticResource CountToVisibilityConverter}}">
                <TextBlock Text="Файлів не знайдено. Завантажте свою першу таблицю!" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Foreground="Gray" FontSize="14"/>
            </Border>

            <!-- Індикатор завантаження -->
            <Border x:Name="LoadingPanel" Grid.Row="1" 
                    Background="#80FFFFFF" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="Завантаження..." FontSize="16" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="10">
            <Button x:Name="OkButton" Content="OK" Width="80" 
                    Margin="0,0,10,0" Click="OkButton_Click" 
                    IsEnabled="False"/>
            <Button Content="Скасувати" Width="80" 
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
